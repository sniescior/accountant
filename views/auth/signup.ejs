<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Accountant - Sign up</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx" crossorigin="anonymous">
    <link rel="stylesheet" href="/public/styles/main.css">
    <link rel="stylesheet" href="/public/styles/auth/auth.css">
</head>
<body>
    <%- include('../partials/_navbar.ejs') %> 
    <div class="wrapper">
        <div class="form-wrapper">
            <h2>Create an account</h2>
            <h5>Let's get you started in just a few steps.</h5>
            <form id="signup-form" action="/auth/signup" method="POST">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">

                <input type="text" placeholder="Username" name="username" value="<%= username %>" required>
                <% if (errors.username) { %>
                    <p class="text-danger m-0"> Username <b>@<%= username %></b> is already in use.</p>
                <% } %>
                <input type="email" placeholder="Email" name="email" value="<%= email %>" required>
                <% if (errors.email) { %>
                    <p class="text-danger m-0">Email <b><%= email %></b> has already been registered.</p>
                <% } %>
                <input type="password" placeholder="Password" name="password" required>
                <% if (errors.password) { %>
                    <p class="text-danger m-0">Password should be at least 8 characters long.</p>
                <% } %>
                
                <input type="password" placeholder="Confirm password" name="confirmPassword" required>
                <% if (errors.confirmPassword) { %>
                    <p class="text-danger m-0">Password doesn't match.</p>
                <% } %>
                
                <button class="bt-primary" type="submit">Continue</button>
            </form>
            <p>
                Joined us before? <a href="/auth/signin">Sign in</a>
            </p>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-A3rJD856KowSb7dwlZdYEkO39Gagi7vIsF0jrRAoQmDKKtQBHUuLZ9AsSv4jD4Xa" crossorigin="anonymous"></script>
    <script src="/public/js/main.js"></script>
</body>
</html>